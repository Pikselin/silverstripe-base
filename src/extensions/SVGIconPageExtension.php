<?php

namespace Pikselin\base {

    use SilverStripe\ORM\DataExtension;

    class SVGIconPageExtension extends DataExtension {
        
        /**
         * Add $db field extension to pull in an id selector, might be best handled directly by the bespoke /app/Page Types method though to keep it agnostic
         */
    }

}
